use super::state::{HelpTab, Overlay};

#[derive(Debug, Clone)]
pub enum AppEvent {
    Noop,
    OpenOverlay(Overlay),
    CloseOverlay,
    SubmitComposer,
    InsertNewline,
    InputChar(char),
    Backspace,
    ScrollTranscript(i32),
    MoveCommandSelection(i32),
    MoveProviderSelection(i32),
    MoveModelSelection(i32),
    MoveAuthModeSelection(i32),
    MoveReasoningEffortSelection(i32),
    MoveResumeSelection(i32),
    SetProviderSelection(usize),
    SetModelSelection(usize),
    SetAuthModeSelection(usize),
    SetReasoningEffortSelection(usize),
    SetResumeSelection(usize),
    SelectPendingOption(usize),
    CycleModelSelection,
    SaveBaseUrlInput,
    SaveApiKeyInput,
    SaveModelNameInput,
    SelectHelpTab(HelpTab),
    ApplyOverlaySelection,
}
